<nav class="navbar navbar-expand navbar-dark bg-dark">
      <div class="div-navbar collapse navbar-collapse">

        <ul class="navbar-nav">
          <li class="nav-item">
            <a id="a-logo" class="nav-link" href="/"></a>
          </li>
          <li class="nav-item <% if(headerMenu == 0){ %> active <% } %>">
            <a class="nav-link" href="/">JsonApi</a>
          </li>
          <li class="nav-item <% if(headerMenu == 1){ %> active <% } %>">
            <a class="nav-link" href="/scheme">Scheme</a>
          </li>
          <li class="nav-item <% if(headerMenu == 2){ %> active <% } %>">
            <a class="nav-link" href="/apns">APNs</a>
          </li>
          <li class="nav-item <% if(headerMenu == 3){ %> active <% } %>">
            <a class="nav-link" href="/uriencodedecode" style="white-space: nowrap;">URI en/de</a>
          </li>
          <li class="nav-item <% if(headerMenu == 4){ %> active <% } %>">
            <a class="nav-link" href="/image">Image</a>
          </li>
          <li class="nav-item mobile-display-none">
            <a class="nav-link" href="https://regexr.com" target="_blank" style="color:#80c0f0;">RegExr</a>
          </li>
        </ul>

        <% if(headerMenu == 0 || headerMenu == 1 || headerMenu == 2) { %>
        <form class="form-inline form-search" action="">
          <div class="input-group">
            <input type="text" class="form-control" name="search" placeholder="SEARCH" aria-label="SEARCH" value="<%= search %>">
            <div class="input-group-append">
              <button class="btn btn-secondary" type="submit"><i class="material-icons size-24 vertical-middle">search</i></button>
            </div>
          </div>
        </form>
        <% } %>

      </div>

      <% if (darkmode) { %>
          <button type="button" class="btn btn-light btn-sm" onclick="document.cookie='darkmode='; location.reload();" title="Default Mode!!"><i class="material-icons">web_asset</i></button>
      <% } else {%>
          <button type="button" class="btn btn-dark btn-sm" onclick="document.cookie='darkmode=true'; location.reload();" title="Dark Mode!!" style="background-color: black;border-color: black;"><i class="material-icons">web_asset</i></button>
      <% } %>

</nav>

<% if(headerMenu == 0 || headerMenu == 1 || headerMenu == 2) { %>
<form class="form-inline form-search-mobile" action="">
  <div class="input-group">
    <input type="text" class="form-control" name="search" placeholder="SEARCH" aria-label="SEARCH" value="<%= search %>">
    <div class="input-group-append">
      <button class="btn btn-secondary" type="submit"><i class="material-icons size-24 vertical-middle">search</i></button>
    </div>
  </div>
</form>
<% } %>
