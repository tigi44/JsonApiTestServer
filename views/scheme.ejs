<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <style>
      a {
        overflow: hidden;
        word-break: break-all;
      }
      #div-list {
        width: 50%;
        float: left;
      }
      #div-edit {
        width: 50%;
        float: left;
      }

      @media ( max-width: 780px ) {
        #div-list {
          width: 100%;
        }
        #div-edit {
          display: none;
        }
      }
    </style>
  </head>
  <body>

    <h1><%= title %></h1>
    <div id="div-list">
    <% include ./schemeList %>
    </div>
    <div id="div-edit">
      <form method="POST">
        <textarea name="schemeHtml" style="width:100%;height:500px;"><% include ./schemeList %></textarea>
        <button type="submit">submit</button>
      </form>
    </div>
</html>
